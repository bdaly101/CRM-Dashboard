<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRM</title>
    <link href="/css/output.css" rel="stylesheet">
</head>

<body class="">

    <header class="flex justify-between items-center px-5 py-4 bg-gray-700 text-white grid-body">
        {{!-- Logo Container --}}
        <div class="nav-aside">
            <a class="text-2xl" href="/contacts">The Best CRM</a>
        </div>
    </header>

    <main class="h-full grid-body">
        <aside id="" class="bg-gray-700 h-screen pt-11 px-5 text-white nav-aside">
            <nav>
                <ul>
                    <li><a href="/contacts">CRM</a></li>
                    <li class="mt-3"><a href="/dashboard">Dashboard</a></li>
                    {{#if logged_in}}
                    <li class="mt-3"><button id="logout">Logout</button></li>
                    {{else}}
                    <li class="mt-3"><a href="/login">Login/SignUp</a></li>
                    {{/if}}
                </ul>
            </nav>
        </aside>

        <section id="" class="main-content h-screen px-5 py-11">
            {{{body}}}
        </section>

    </main>

    <script src="/js/logout.js"></script>

</body>

</html>